{
  "name": "mi-crm-vibecoder",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page de Nexo CRM",
  "scripts": {
    "start": "serve -s . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.3"
  }
}
